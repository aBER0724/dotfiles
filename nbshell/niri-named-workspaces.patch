diff --git a/shell/Bar/Bar.qml b/shell/Bar/Bar.qml
index a7a2296..a654efe 100644
--- a/shell/Bar/Bar.qml
+++ b/shell/Bar/Bar.qml
@@ -194,7 +194,7 @@ Variants {
                 return closedWidth + (openWidth - closedWidth) * win.openProgress;
             }

-            radius: win.barMode ? 0 : Theme.radius
+            radius: Theme.radius
             color: Theme.alpha(Theme.bg, Config.barOpacity)
             border.width: Config.barBorder ? Theme.borderWidth : 0
             border.color: Theme.muted
diff --git a/shell/Bar/Widgets/CalendarPanel.qml b/shell/Bar/Widgets/CalendarPanel.qml
index 5393577..c6711ca 100644
--- a/shell/Bar/Widgets/CalendarPanel.qml
+++ b/shell/Bar/Widgets/CalendarPanel.qml
@@ -322,7 +322,7 @@ Column {
     Line {
         visible: !Calendar.available
         width: panel.rowWidth
-        text: "  " + (Calendar.problem === "khal fehlt" ? "khal is not installed — pacman -S khal vdirsyncer" : Calendar.problem)
+        text: "  " + (Calendar.problem === "khal fehlt" ? "khal is not installed — calendar sync is unavailable" : Calendar.problem)
         color: Theme.yellow
         wrapMode: Text.WordWrap
     }
diff --git a/shell/Bar/Widgets/Tray.qml b/shell/Bar/Widgets/Tray.qml
index e9ae767..3db5b93 100644
--- a/shell/Bar/Widgets/Tray.qml
+++ b/shell/Bar/Widgets/Tray.qml
@@ -40,7 +40,7 @@ Cell {
             id: toggle

             anchors.verticalCenter: parent.verticalCenter
-            text: root.expanded ? "<" : ">"
+            text: root.expanded ? ">" : "<"
             color: Theme.textDim

             MouseArea {
diff --git a/shell/Menu/Dashboard.qml b/shell/Menu/Dashboard.qml
index df3d5cc..519739b 100644
--- a/shell/Menu/Dashboard.qml
+++ b/shell/Menu/Dashboard.qml
@@ -282,7 +282,7 @@ PanelWindow {
                             title: "Upcoming events"
                             badge: Calendar.loading ? "…" : "OPEN  ·  " + String(root.nextEvents.length)
                             run: () => root.page = 3
-                            Line { visible: root.nextEvents.length === 0; text: Calendar.available ? "nothing in the next few days" : Calendar.problem; color: Theme.muted }
+                            Line { visible: root.nextEvents.length === 0; text: Calendar.available ? "nothing in the next few days" : (Calendar.problem === "khal fehlt" ? "calendar sync unavailable" : Calendar.problem); color: Theme.muted }
                             Repeater {
                                 model: root.nextEvents.slice(0, 5)
                                 Item {
diff --git a/shell/Services/Compositor.qml b/shell/Services/Compositor.qml
index 27aeef4..d272752 100644
--- a/shell/Services/Compositor.qml
+++ b/shell/Services/Compositor.qml
@@ -33,7 +33,7 @@ Singleton {
     readonly property string focusedAppId: focusedWindow?.app_id ?? ""

     function workspacesForOutput(output) {
-        return workspaces.filter(w => !output || !w.output || w.output === output).sort((a, b) => a.idx - b.idx);
+        return workspaces.filter(w => (!output || !w.output || w.output === output) && (!isNiri || String(w.name || "") !== "")).sort((a, b) => a.idx - b.idx);
     }

     function focusWorkspace(value) {
diff --git a/shell/Services/Niri.qml b/shell/Services/Niri.qml
index 3e7ca9b..e9f807f 100644
--- a/shell/Services/Niri.qml
+++ b/shell/Services/Niri.qml
@@ -43,7 +43,7 @@ Singleton {
     readonly property string focusedAppId: focusedWindow?.app_id ?? ""

     function workspacesForOutput(output) {
-        return workspaces.filter(w => !output || w.output === output).sort((a, b) => a.idx - b.idx);
+        return workspaces.filter(w => (!output || w.output === output) && String(w.name || "") !== "").sort((a, b) => a.idx - b.idx);
     }

     function focusWorkspace(idx) {
